{
  "tag": "sb-details",
  "props": [
    {
      "name": "summary",
      "attribute": "summary",
      "type": "string",
      "default": "Details",
      "required": false,
      "docs": {
        "description": "The heading on the summary row. The server can morph it; the summary slot overrides it."
      }
    },
    {
      "name": "open",
      "attribute": "open",
      "type": "boolean",
      "default": false,
      "required": false,
      "docs": {
        "description": "Open. View state, not a value: a changed attribute from the server wins, a removed one is ignored (send open=\"false\" to close). Local toggling never reflects it."
      }
    },
    {
      "name": "icon",
      "attribute": "icon",
      "type": "string",
      "default": "",
      "required": false,
      "docs": {
        "description": "Short text or emoji before the summary (decorative)."
      }
    },
    {
      "name": "disabled",
      "attribute": "disabled",
      "type": "boolean",
      "default": false,
      "required": false,
      "docs": {
        "description": "Block toggling. The server can still open or close it."
      }
    },
    {
      "name": "group",
      "attribute": "group",
      "type": "string",
      "default": "",
      "required": false,
      "docs": {
        "description": "Accordion group: opening one sb-details with this group name closes the others, and each of them emits its own sb-toggle."
      }
    },
    {
      "name": "name",
      "attribute": "name",
      "type": "string",
      "default": "",
      "required": false,
      "docs": {
        "description": "Name reported in sb-toggle (e.g. the field of a command)."
      }
    }
  ],
  "slots": [
    {
      "name": "default",
      "description": "The content the panel reveals."
    },
    {
      "name": "summary",
      "description": "Rich content for the summary row; replaces the summary prop."
    }
  ],
  "events": [
    {
      "kind": "custom-event",
      "name": "sb-toggle",
      "bubbles": true,
      "composed": true,
      "description": "The panel opened or closed. detail: { name, open }. View state: never pending, and not emitted for a change the server itself made."
    }
  ]
}
