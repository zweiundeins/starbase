# Turns a "Submit a component" issue into a pull request.
#
# Security model: submitted code is untrusted. It only ever runs in the
# `build` job (read-only token, no persisted git credentials), where it is
# written to disk, loaded in headless Chrome to generate its manifest and
# validated. The `pull-request` job has write access but never executes
# submitted code; it only copies the files `build` produced.
#
# Note: PRs opened with GITHUB_TOKEN don't trigger other workflows. CI is
# not needed for them (build already ran the same checks); to run it anyway,
# use "Run workflow" on CI or give this workflow a GitHub App token.
name: Component from issue

on:
  issues:
    types: [opened, edited, labeled, reopened]

permissions: {}

concurrency:
  group: component-issue-${{ github.event.issue.number }}
  cancel-in-progress: true

jobs:
  build:
    if: contains(github.event.issue.labels.*.name, 'new-component') && github.event.issue.state == 'open'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: read
    outputs:
      slug: ${{ steps.build.outputs.slug }}
      tag: ${{ steps.build.outputs.tag }}
      name: ${{ steps.build.outputs.name }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version-file: go.mod
      - name: Build component folder from the issue form
        id: build
        # The issue is read from $GITHUB_EVENT_PATH, never interpolated here.
        run: go run ./cmd/fromissue --report report.md --notes notes.md
        env:
          GITHUB_TOKEN: ${{ github.token }} # read-only; raises the API limit for source repos
          STARBASE_URL: ${{ vars.STARBASE_URL }} # playground links are only imported from here
      - name: Let Chrome use its sandbox
        # Ubuntu 24.04 restricts unprivileged user namespaces, which Chrome's
        # sandbox needs; without this it would have to run with --no-sandbox.
        run: sudo sysctl -w kernel.apparmor_restrict_unprivileged_userns=0 || true
      - name: Generate manifest in headless Chrome
        run: go run -tags dev ./cmd/manifests
      - name: Validate
        run: go test ./internal/catalog/ ./internal/submission/
      - name: Keep the result
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: submission
          path: |
            report.md
            notes.md
            components/${{ steps.build.outputs.slug || 'none' }}/
          if-no-files-found: ignore
          retention-days: 3

  pull-request:
    needs: build
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: write
      pull-requests: write
      issues: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: submission
          path: .submission
      - name: Copy the generated folder and write the PR body
        env:
          SLUG: ${{ needs.build.outputs.slug }}
          AUTHOR: ${{ github.event.issue.user.login }}
          ISSUE: ${{ github.event.issue.number }}
        run: |
          # Never trust the build job's outputs blindly (it ran submitted code):
          # a valid slug, and only regular files of the expected kinds.
          [[ "$SLUG" =~ ^[a-z][a-z0-9]*(-[a-z0-9]+)*$ ]] || { echo "::error::bad slug"; exit 1; }
          src=".submission/components/$SLUG"
          if [ -n "$(find "$src" ! -type f ! -type d)" ]; then echo "::error::unexpected non-regular files"; exit 1; fi
          rm -rf "components/$SLUG"
          (cd "$src" && find . -type f \( -name '*.js' -o -name '*.mjs' -o -name '*.md' -o -name '*.json' \) -print0) |
            while IFS= read -r -d '' f; do install -D -m 644 "$src/$f" "components/$SLUG/$f"; done
          {
            echo "Submitted by @$AUTHOR in #$ISSUE. Generated from the issue form: validated, with its manifest generated in headless Chrome. The bot comments a playground preview link for every revision."
            echo
            cat .submission/notes.md 2>/dev/null || echo "No reviewer notes."
            echo
            echo "### Review checklist"
            echo
            echo "- [ ] Not a duplicate of an existing component (see above)"
            echo "- [ ] Works in the playground preview, and looks right in every theme there"
            echo "- [ ] Docs examples work, Playground makes sense"
            echo "- [ ] No network access; imports only \`datastar\` and its own folder (vendored files: official builds, compatible license)"
            echo
            echo "Closes #$ISSUE"
          } > "$RUNNER_TEMP/body.md"
          rm -rf .submission
      - name: Open or update the pull request
        id: pr
        uses: peter-evans/create-pull-request@5f6978faf089d4d20b00c7766989d076bb2fc7f1 # v8.1.1
        with:
          branch: component/${{ needs.build.outputs.slug }}
          add-paths: components/${{ needs.build.outputs.slug }}
          commit-message: "Add <${{ needs.build.outputs.tag }}> from #${{ github.event.issue.number }}"
          author: ${{ github.event.issue.user.login }} <${{ github.event.issue.user.id }}+${{ github.event.issue.user.login }}@users.noreply.github.com>
          title: "Add <${{ needs.build.outputs.tag }}>: ${{ needs.build.outputs.name }}"
          labels: new-component
          body-path: ${{ runner.temp }}/body.md
      - name: Link the preview and tell the author
        env:
          GH_TOKEN: ${{ github.token }}
          ISSUE: ${{ github.event.issue.number }}
          PR: ${{ steps.pr.outputs.pull-request-number }}
          URL: ${{ steps.pr.outputs.pull-request-url }}
          OP: ${{ steps.pr.outputs.pull-request-operation }}
          SHA: ${{ steps.pr.outputs.pull-request-head-sha }}
          SLUG: ${{ needs.build.outputs.slug }}
          SITE: ${{ vars.STARBASE_URL }}
        run: |
          [ "$OP" = created ] || [ "$OP" = updated ] || exit 0 # unchanged: nothing to say
          preview=""
          if [ -n "$SITE" ] && [ -n "$SHA" ]; then
            preview="${SITE%/}/playground?preview=$SHA/$SLUG"
            gh pr comment "$PR" --repo "$GITHUB_REPOSITORY" --body "🔭 Try this revision (\`${SHA:0:7}\`) in the playground: $preview"
          fi
          case "$OP" in
            created) msg="🚀 Your component is on the launch pad: $URL. A maintainer will review it. Edit this issue to update the pull request." ;;
            updated) msg="🔁 Pull request updated with your changes: $URL" ;;
          esac
          [ -n "$preview" ] && msg="$msg

          Try it in the playground: $preview"
          gh issue comment "$ISSUE" --repo "$GITHUB_REPOSITORY" --body "$msg"

  report-failure:
    needs: [build, pull-request]
    if: failure()
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      issues: write
    steps:
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        continue-on-error: true
        with:
          name: submission
          path: .submission
      - name: Comment the problems
        env:
          GH_TOKEN: ${{ github.token }}
          ISSUE: ${{ github.event.issue.number }}
          RUN: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
          BUILT: ${{ needs.build.result }}
        run: |
          if [ "$BUILT" = success ]; then
            body="✅ Your component is valid, but the pull request could not be opened. A maintainer will take it from here (run: $RUN)."
          elif [ -s .submission/report.md ] && ! grep -q '^✅' .submission/report.md; then
            body="$(cat .submission/report.md)"
          else
            body="🛑 The component was built but failed validation or manifest generation (does it load without errors?). Details: $RUN

          Edit this issue to try again."
          fi
          gh issue comment "$ISSUE" --repo "$GITHUB_REPOSITORY" --body "$body"
